# UniAI Environment Variables Configuration Example
# Copy this file to .env and fill in actual values

# DeepSeek API Key
# Get from https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Scene prompt configuration (JSON format)
# Define system prompts for different scenarios
SYSTEM_PROMPTS={
  "scene_1": "You are a professional translator, please translate the user's input into English.",
  "scene_2": "You are a code assistant, please optimize the code provided by the user.",
  "scene_3": "You are a writing assistant, please help users improve their articles."
}

# Supported model list (JSON array format)
# Define AI models supported by the system
SUPPORTED_MODELS=["deepseek-chat"]

# Application configuration
# Application port
PORT=8000

# Application environment (development, production, testing)
ENVIRONMENT=development

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# CORS allowed origins (should set specific domains in production)
CORS_ORIGINS=["*"]

# API rate limiting configuration
# Maximum requests per minute
RATE_LIMIT_PER_MINUTE=60

# Database configuration (if needed)
# DATABASE_URL=sqlite:///./app.db
# DATABASE_URL=postgresql://user:password@localhost/dbname

# Redis configuration (if caching is needed)
# REDIS_URL=redis://localhost:6379/0

# JWT secret key (if authentication is needed)
# JWT_SECRET_KEY=your_jwt_secret_key_here
# JWT_ALGORITHM=HS256
# JWT_EXPIRE_MINUTES=30